spring:
  application:
    name: ai-recommendation-service-test

server:
  servlet:
    context-path: /ai-service-ms

app:
  integration:
    mode: mock
  security:
    jwt:
      secret: "01234567890123456789012345678901"
  llm:
    enabled: false
    base-url: http://localhost:11434/v1
    model: llama3.2
    read-timeout-ms: 60000
