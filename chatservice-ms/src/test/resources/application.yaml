spring:
  application:
    name: chatservice-ms-test

  datasource:
    url: jdbc:h2:mem:chatservice_test;MODE=MySQL;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop

app:
  security:
    jwt-secret: test-jwt-secret-for-unit-tests-min-32-characters-long

chat:
  order-service:
    base-url: http://localhost:59999
